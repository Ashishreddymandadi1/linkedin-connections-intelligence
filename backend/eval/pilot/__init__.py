"""V4 PART 10 — controlled real-data pilot + quality evaluation.

Everything here is OFFLINE by default. Nothing in this package scrapes,
enriches from Apify, or mutates the production dataset. Live LLM calls happen
ONLY through ``run_pilot.py --live`` after an explicit human go-ahead.
"""
